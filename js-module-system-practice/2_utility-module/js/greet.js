// js/greet.js
// 인사 함수 모듈 — 이 파일의 "대표 선수"를 내보내세요.

// TODO: 아래 함수를 default export 하세요.
// hint: function 앞에 export default 키워드를 추가하면 됩니다.
// 예시: export default function 함수이름(매개변수) { ... }

function greet(name) {
  return `안녕하세요, ${name}님!`;
}
