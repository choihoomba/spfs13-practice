// js/config.js
// 사이트 설정 객체 모듈 — 설정 객체를 default export 하세요.

// TODO: 아래 객체를 default export 하세요.
// hint: 객체 앞에 export default를 붙이면 됩니다.
// 예시: export default { key: 'value' };

const siteConfig = {
  siteName: "나의 블로그",
  maxPosts: 10,
  theme: "dark",
  language: "ko",
};

export default siteConfig;
