// 1. forEach 직접 구현
function forEach(array, callback) {
  // TODO: 배열의 각 요소에 대해 callback 실행
}

forEach([1, 2, 3], function (item, index) {
  console.log(`${index}: ${item}`);
});
// 0: 1
// 1: 2
// 2: 3
