// 1. 다음 함수들을 Arrow Function으로 변환하세요

// 기존
function double(n) {
  return n * 2;
}

// TODO: Arrow Function으로
const double = (n) => n * 2;

// 기존
function greet(name) {
  return "Hello, " + name;
}

// TODO: Arrow Function으로
const greet = (name) => `Hello, ${name}`;

// 기존
function sum(a, b, c) {
  return a + b + c;
}

// TODO: Arrow Function으로
const sum = (a, b, c) => a + b + c;
